<fileHeader>

#warning("Manually move this test file to the corresponding test target.")

import <testImport1>
import <testImport2>

final class <nodeName>ViewControllerTests: XCTestCase {

    func testNotNil() {
        let viewController: <nodeName>ViewController = givenViewController(with: <nodeName>ViewState())
        expect(viewController) != nil
    }

    private func givenViewController(
        with state: <nodeName>ViewState
    ) -> <nodeName>ViewController {
        <nodeName>ViewController(
            initialState: state,
            <viewStatePropertyName>: <viewStateEmptyFactory>
        )
    }
}
