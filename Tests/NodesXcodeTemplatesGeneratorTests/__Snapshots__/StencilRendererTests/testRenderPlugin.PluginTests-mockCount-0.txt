<fileHeader>

#warning("Manually move this test file to the corresponding test target then delete this warning.")

final class <pluginName>PluginTests: XCTestCase {

    private var plugin: <pluginName>PluginImp!

    override func setUp() {
        super.setUp()
        plugin = .init(componentFactory: injectComponent {
            <pluginName>PluginComponent(parent: $0)
        } with: {
            let dependency: <pluginName>PluginDependencyMock = .init()
            return dependency
        })
    }

    override func tearDown() {
        plugin = nil
        super.tearDown()
    }

    func testPluginIsNotEnabled() {
        XCTAssertNil(plugin.create())
    }

    func testOverride() {
        XCTAssertTrue(plugin.override() is <pluginName>Builder)
    }
}
