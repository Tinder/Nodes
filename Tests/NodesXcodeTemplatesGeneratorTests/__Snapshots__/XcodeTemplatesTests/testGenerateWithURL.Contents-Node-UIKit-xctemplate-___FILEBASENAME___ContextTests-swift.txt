//___FILEHEADER___

// add @testable import 'your_module' and delete this comment
// add @testable import 'your_module'_GeneratedMocks and delete this comment
import XCTest
// MOVE THESE TEST FILES TO YOUR MODULE'S LIBRARY TESTS FOLDER

final class ___VARIABLE_productName___ContextTests: XCTestCase {

    private var context: ___VARIABLE_productName___ContextImp!
    private var analyticsMock: ___VARIABLE_productName___AnalyticsMock!

    override func setUp() {
        super.setUp()
        analyticsMock = ___VARIABLE_productName___AnalyticsMock()
        context = .init(
            presentable: ___VARIABLE_productName___PresentableMock(),
            workers: [],
            analytics: analyticsMock
        )
    }

    override func tearDown() {
        context = nil
        analyticsMock = nil
        super.tearDown()
    }

    func testNotNil() {
        XCTAssertNotNil(context)
    }

    func testWorkersEmpty() {
        XCTAssertEqual(context.workers.count, 0)
    }

    func testActivate() {
        context.activate()
        XCTAssertTrue(context.isActive)
    }

    func testDeactivate() {
        context.activate()

        context.deactivate()
        XCTAssertFalse(context.isActive)
    }
}